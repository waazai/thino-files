import { Plugin } from "obsidian";

export default class ThinoFilesPlugin extends Plugin {
  async onload(): Promise<void> {
    // View, command, settings registered in later slices.
  }

  onunload(): void {}
}
